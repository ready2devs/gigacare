[package]
name = "gigacare-fs"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Motor de recorrido recursivo de árbol de directorios con cálculo de tamaños, detección de symlinks y cancelación"

[dependencies]
walkdir = "2"
globset = "0.4"
thiserror = "2"

[dev-dependencies]
tempfile = "3"